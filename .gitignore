# סודות — לעולם לא לריפו
.streamlit/secrets.toml

# נתונים מקומיים — המאגר חי ב-Google Sheets / בתיקייה השיתופית, לא בריפו
מאגר בקרות*.xlsx
config.json
גיבוי מאגר/

copy_secrets.py
_push_log.txt

__pycache__/
*.pyc
